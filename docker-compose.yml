services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: alerta
      POSTGRES_PASSWORD: alerta123
      POSTGRES_DB: alerta_licitaciones
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
